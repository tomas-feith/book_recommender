# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
.venv39/
venv/

# Dataset build cache (downloaded source data)
.cache/

# Secrets
.env

# User state — per-user swipe log, regenerated at runtime
data/app.db

# Sparse CF matrix — large & regenerable (build_real_dataset.py / refresh.py)
data/real_cf.npz

# Co-read fine-tuned encoder — large (~130MB) & regenerable (finetune_coread.py)
data/coread-encoder/
data/coread-encoder.ckpt*.pt
